var should = require('should');

describe('bem-data-source', function () {
    before(function () {});

    it('should be ok', function () {
        console.log('ok');
    });

    after(function () {});
});
